
# vimalaya

Neovim UI for [Pimalaya](https://github.com/pimalaya)'s CLI tools:
- `himalaya` for email
- `calendula` for calendars
- `cardamum` for contacts

This project assumes that the maildir backend will be used with himalaya for
fast searching of emails with `ripgrep`.
